# 市场部工作看板系统：生产规格

> 状态：已确认，可作为实现与验收依据  
> 目标版本：Release 1 / Release 1.1  
> 默认时区：`Asia/Shanghai`  
> 默认语言：简体中文

## 1. 产品目标

建设一套供市场部长期真实使用的生产系统，覆盖员工日报、周报、计划滚动、项目聚合、阻塞协调和管理分析。

系统必须同时支持：

- 员工高效填写、修改和追踪自己的日报、周报与计划。
- 老板从成员视角和项目视角查看提交、计划、交付物和阻塞情况。
- 管理员维护账号、项目、分类、单位、工作日历与系统配置。
- 多账号通过公网访问同一份权威数据。
- 完整的权限控制、版本历史、操作审计、备份和恢复能力。

本系统不是单机 Demo。`LocalStorage` / `IndexedDB` 不得作为正式业务数据源，只能用于未提交草稿和短期查询缓存。

### 1.1 成功标准

老板打开首页后，应能按以下顺序快速回答：

1. 今天谁尚未提交。
2. 哪些事项被阻塞、需要谁协调。
3. 各项目本周完成了什么、下周计划什么。
4. 计划与实际有哪些偏差。
5. 交付物数量、阻塞趋势和成员负荷是否异常。

所有管理指标必须能下钻到报告、任务或阻塞原文。

### 1.2 明确非目标

- 不把任务数、提交率或交付物数量直接解释为绩效。
- 不生成员工排行榜。
- 没有里程碑和权重前，不展示伪精确的项目完成百分比。
- Release 1 不集成邮件、短信或企业 IM 推送。
- Release 1 不实现多租户管理界面。
- 不支持 Internet Explorer。

## 2. 当前项目基线与迁移约束

当前项目已建立可运行基线：

- Next.js 16 App Router、React 19、TypeScript 严格模式和 Tailwind CSS v4。
- PostgreSQL、版本化迁移、Better Auth、Vitest 与 Playwright 验证链路。
- BEUI Pro 私有 Registry 已通过 `components.json` 接入，组件以源码形式纳入项目。
- BEUI Pro 生成源码集中在 `src/components/premium` 与 `src/components/motion`，共享工具位于 `src/lib`。
- `@/*` 路径别名、Design Tokens、开发环境种子数据和本地基础设施脚本均已建立。

后续迁移必须：

1. 保留现有服务端数据、权限和领域逻辑，只替换或收敛前端视觉与交互层。
2. 新增组件优先从 BEUI Pro 私有 Registry 获取，再按本项目中文业务场景适配。
3. 旧组件只有在调用方全部迁移且验证通过后才能删除，不得静默破坏导入关系。
4. React、React DOM 和所有运行时依赖必须保持为直接依赖并锁定版本。
5. 不提交 `node_modules`、授权令牌、密钥、数据库文件、用户附件或生产数据。

## 3. 交付分期

### 3.1 Release 1

- 用户名密码登录和会话管理。
- 员工、老板、管理员三类角色及服务端权限控制。
- 日报、周报、计划滚动和修订版本。
- 成员视角与项目视角看板。
- 阻塞中心和协调流程。
- 用户、项目、分类、单位与工作日历管理。
- 站内通知中心。
- 搜索、筛选和分页。
- 业务审计、安全审计、备份与恢复。
- 开发、预发布、生产三套环境。

### 3.2 Release 1.1

- 评论、回复和 `@成员`。
- 图片、小型文档附件和外部交付链接。
- Excel / JSON 导入导出。
- 可分享的筛选链接。
- 字典项合并与撤销窗口。
- 超期修订申请流程增强。

Release 1.1 功能不得在 Release 1 中以不可用按钮、假数据或占位逻辑出现。

## 4. 技术架构

### 4.1 应用架构

- 全栈框架：Next.js + TypeScript，使用实施时的稳定版本。
- 数据库：PostgreSQL，作为唯一权威业务数据源。
- 数据访问：成熟 ORM，使用版本化迁移；禁止生产环境自动同步 schema。
- 输入校验：服务端 schema 校验；客户端校验只改善体验，不能替代服务端校验。
- 身份认证：支持用户名密码和数据库会话的成熟认证方案。
- 后台任务：用于提醒、日志清理、备份检查和周期性周报草稿生成。
- Release 1.1 对象存储：私有 S3 兼容存储。

具体依赖和版本在安装前必须核对当前官方文档并锁定；不得依赖模型记忆选择“最新”版本。

### 4.2 数据与缓存

- 正式提交必须成功写入 PostgreSQL 后才能显示“已提交”。
- 浏览器存储只允许保存未提交草稿、用户偏好和可丢弃缓存。
- 离线时必须明确显示未同步状态，不得伪造成功提交。
- 服务端列表必须分页，不一次性加载全部历史数据。
- 生产环境不得自动写入 Mock 数据。
- 开发和预发布环境使用生成数据或脱敏数据，严禁复制生产明文数据。

### 4.3 API 与授权

- 每个读取和写入接口都必须在服务端校验身份、角色、资源归属和组织边界。
- 前端隐藏菜单不构成权限控制。
- 所有修改接口必须验证输入、处理并发版本并记录审计上下文。
- 关键写操作必须具备幂等性或重复提交保护。
- 所有实体预留 `organizationId`，第一版只启用一个组织。

## 5. UI 与设计系统

### 5.1 BEUI Pro 使用约束

BEUI Pro 通过授权私有 Registry 提供组件源码，不作为运行时黑盒包使用。

- Registry 配置统一维护在 `components.json`，命名空间为 `@beui-pro`；社区组件使用 `@beui`。
- 授权只通过用户级或 CI 密钥环境变量 `BEUI_PRO_TOKEN` 注入，禁止写入源码、文档、日志、截图或 Git 历史。
- 新组件使用 `npx shadcn@latest add @beui-pro/{name}` 获取，并将生成源码、依赖和锁文件一并审查、测试和提交。
- 高级业务组件放在 `src/components/premium`，动画基础组件放在 `src/components/motion`，共享 Hook 与工具放在 `src/lib`。
- 工作台壳层优先使用 Animated Sidebar；登录使用 Auth Split；项目摘要使用 Compact Card；管理列表优先使用 Responsive Data Table；空数据和无搜索结果使用对应 Empty State。
- Button、Select、Input、Textarea、Dialog、Card、Badge、Tabs、DatePicker、Dropdown 和 Table 等基础控件不得在业务页面重复手写。
- 交互底层优先复用 Radix UI Headless Components；复杂数据逻辑优先使用 TanStack 系列能力，图表使用 Recharts，动画统一使用 Motion。
- 业务代码只从本地 `@/components/...` 路径导入 BEUI Pro 源码，避免页面依赖远程 Registry 的运行时可用性。
- Registry 组件必须适配简体中文、现有权限与真实接口；不得保留英文演示文案、静态提交成功、无效按钮或与业务无关的 Mock 状态。
- 对 Registry 源码的产品化修改应保持接口清晰，避免在具体业务页面复制组件内部实现。

### 5.2 Design Tokens 与组件状态

- 颜色、字体、间距、圆角、边框、阴影、层级和动画时序统一由 `src/styles/theme.css` 管理。
- BEUI Pro 使用 `background`、`foreground`、`card`、`muted`、`primary`、`border`、`ring` 和 `destructive` 等语义 Token；这些 Token 必须映射到本项目现有主题变量。
- 业务页面不得硬编码品牌色、状态色、阴影或圆角值；动态数据宽度等非主题数值除外。
- 重复出现的页面标题、数据表、筛选栏、空状态、状态标识和分页应沉淀为共享组件。
- 组件必须覆盖默认、悬停、激活、聚焦、禁用、加载、成功、失败、空状态和响应式布局。
- 动画必须服务于状态变化，并遵循 `prefers-reduced-motion`；不得用持续动画干扰高频操作。

### 5.3 视觉原则

- 清晰、克制、高信息密度、适合频繁扫描与重复操作。
- 桌面端优先覆盖 1280-1920px；移动端保证填报、筛选和查看可用。
- 窄屏中的复杂表格改为纵向任务列表，不强行压缩桌面表格。
- 以浅色主题为第一优先，同时保留语义 Token 的深色主题扩展能力。
- 页面区块采用平面信息层级，卡片只用于独立重复项、工具或模态内容，禁止卡片嵌套卡片。
- 圆角、阴影和强调色保持克制；状态同时使用文本、图标或形状，不只依靠颜色。
- 状态不得只依靠颜色表达，必须同时有图标和文本。
- 键盘操作、焦点状态、表单错误和读屏语义达到 WCAG 2.1 AA。

状态色必须使用语义 Token：

- 完成：`status-lime-text`
- 推进中 / 常规计划：`primary` 或 `status-blue-text`
- 待跟进 / 延期：`status-yellow-text` 或 `status-orange-text`
- 风险 / 阻塞 / 需协调：`destructive` 或 `status-rose-text`

## 6. 角色与权限

角色保持分离：

| 能力 | 员工 `EMPLOYEE` | 老板 `BOSS` | 管理员 `ADMIN` |
| --- | --- | --- | --- |
| 编辑本人草稿 | 是 | 仅本人 | 仅本人 |
| 查看他人草稿正文 | 否 | 否 | 否 |
| 查看团队已提交报告 | 是 | 是 | 否，除非使用单独的 `BOSS` 账号 |
| 修改本人已提交报告 | 按修订规则 | 按修订规则 | 按修订规则 |
| 修改他人报告 | 否 | 通过受审计修订流程 | 否 |
| 查看管理看板 | 基础团队视图 | 完整 | 系统运营所需视图 |
| 分配阻塞协调负责人 | 否 | 是 | 否 |
| 管理账号 | 否 | 否 | 是 |
| 管理项目、分类、单位、日历 | 否 | 否 | 是 |
| 查看完整系统审计 | 否 | 仅业务变更 | 是 |

同一个自然人可以分别持有老板账号和管理员账号，但两个角色的权限和审计身份不能混合。

### 6.1 内容可见性

- 草稿仅作者可见；老板只能看到“未提交”状态，不能查看正文。
- 已提交报告默认对团队成员可见。
- 敏感阻塞只对提出人、协调负责人和老板可见。
- 管理员不能利用后台权限读取无业务必要的报告正文。
- 搜索、导出和通知必须继承相同的可见性规则。

## 7. 认证与账号安全

### 7.1 账号生命周期

- 用户名全局唯一，由管理员创建。
- 管理员创建账号时直接设置正式密码，账号创建后立即启用。
- 忘记密码由管理员输入新的正式密码完成重置；重置密码不改变账号启停状态。
- 账号只能停用，不能物理删除。
- 停用账号的历史报告、任务、评论和审计署名永久保留。
- 未完成任务可由管理员批量转交，历史报告署名不可更改。

### 7.2 密码与会话

- 密码至少 12 位，服务端使用强密码哈希算法。
- 会话 Cookie 必须设置 `HttpOnly`、`Secure` 和适当的 `SameSite`。
- 修改密码、管理员重置密码或停用账号后撤销其他会话。
- 禁止通用后门、默认密码和日志中的明文凭证。

### 7.3 初始管理员

- 首个管理员通过一次性部署命令创建。
- 初始化命令通过标准输入接收正式密码，不得写入应用日志、Shell 历史或版本库。
- 初始化完成后账号立即启用。

## 8. 核心领域模型

服务端数据库 schema 是权威模型；共享 TypeScript 类型用于应用边界。所有业务实体至少包含稳定 ID、`organizationId`、创建时间和更新时间。支持软删除的实体使用 `deletedAt`。

### 8.1 核心枚举

```typescript
export type UserRole = "EMPLOYEE" | "BOSS" | "ADMIN";
export type UserStatus = "ACTIVE" | "DISABLED";

export type ReportType = "DAILY" | "WEEKLY";
export type ReportStatus = "DRAFT" | "SUBMITTED";

export type TaskStatus =
  | "TODO"
  | "IN_PROGRESS"
  | "BLOCKED"
  | "DONE"
  | "CANCELED";
export type TaskKind = "ACTUAL" | "PLAN";

export type ProjectStatus =
  | "PLANNED"
  | "ACTIVE"
  | "ON_HOLD"
  | "COMPLETED"
  | "ARCHIVED";

export type BlockerSeverity = "NORMAL" | "IMPORTANT" | "URGENT";
export type BlockerStatus = "OPEN" | "ACKNOWLEDGED" | "RESOLVED";
```

### 8.2 实体定义

#### Organization

- `id`, `name`, `timezone`, `locale`
- 第一版只创建一个组织，但所有业务实体保留组织边界。

#### User

- `id`, `organizationId`, `username`, `displayName`
- `role`, `status`, `title`, `avatarUrl?`
- 密码哈希和最后登录信息不得混入公开用户 DTO。

#### Project

- `id`, `organizationId`, `name`, `description?`
- `ownerId`, `status`, `startDate?`, `targetEndDate?`
- 参与成员通过关联表维护。
- 归档项目保留历史，但不能被新任务选择。

#### Category / DeliverableUnit

- 稳定 ID、名称、是否启用、排序值。
- 停用而不是物理删除。
- 历史任务保存名称快照。
- Release 1.1 支持受审计的合并和撤销窗口。

#### WorkTask

- `id`, `organizationId`, `createdById`, `projectId`, `categoryId`
- `content`, `kind`, `status`, `workDate?`, `dueDate?`
- `sourceTaskId?`：计划滚动或计划转实际时保留来源链路。
- `primaryAssigneeId`：唯一主负责人。
- 协作人通过关联表维护。
- 使用版本字段处理并发修改。

#### Deliverable

- `id`, `taskId`, `unitId`, `quantity`
- 同一任务可有多项交付物。
- 项目汇总按单位分别统计，不跨单位相加。
- 协作人引用任务时不重复计算交付物。

#### Report

- `id`, `organizationId`, `authorId`, `type`, `status`
- 日报使用 `reportDate`；周报使用 `weekStart`、`weekEnd` 和周五 `weekLabel`。
- `dueAt`, `submittedAt?`, `lastEditedAt`, `revisionNumber`, `wasLate`
- `noWorkReason?`, `noPlanReason?`：区分真实空内容与漏填。
- 日报和周报通过关联表引用任务及提交时快照。
- 周报必须保留来源日报与来源任务关系。

#### ReportRevision

- 报告每次正式修订生成不可覆盖的新版本。
- 保存修订人、时间、原因和结构化差异。
- 老版本不可修改或删除。

#### RevisionRequest

- 用于提交超过 7 天后的报告修订申请。
- 保存申请人、报告、修订原因、拟变更内容、审核人、审核结果和时间。
- 审核通过后生成新的 `ReportRevision`，不得直接覆盖原报告。

#### Blocker

- `id`, `organizationId`, `reporterId`, `coordinatorId?`
- `projectId?`, `taskId?`, `severity`, `status`
- `description`, `isSensitive`, `createdAt`, `acknowledgedAt?`, `resolvedAt?`
- `resolution?`

#### Notification

- `id`, `recipientId`, `type`, `title`, `body`, `link?`
- `readAt?`, `createdAt`
- 通知不复制敏感内容；打开目标资源时再次校验权限。

#### Comment（Release 1.1）

- 支持报告、任务和阻塞项评论、回复及 `@成员`。
- 作者可编辑自己的评论，并显示修改标记。
- 删除使用软删除并保留审计记录。

#### Attachment（Release 1.1）

- 数据库只存元数据、对象键、文件类型、大小、哈希和关联资源。
- 对象存储使用私有桶和短期签名链接。
- 限制类型和大小，并进行恶意文件检查。
- 大型视频使用公司已有云盘外部链接，不直接托管。

#### WorkCalendarDay

- 日期、是否工作日、节假日/调休/公司特殊日类型和说明。
- 日历版本化，提醒和逾期计算必须引用当时有效版本。

#### ReportingExemption

- `userId`, `startDate`, `endDate`, `reason`, `createdById`
- 用于请假、免报和其他管理员确认的例外。
- 有效免报日期不进入应提交人数和逾期统计。

#### AuditLog

- 操作者、动作、资源类型、资源 ID、结果、时间和必要上下文。
- 敏感字段必须脱敏，禁止记录密码和会话令牌。
- 审计记录不可通过普通业务界面修改或删除。

## 9. 核心业务规则

### 9.1 日报

- 工作日 18:30 前提交，17:30 生成站内预提醒。
- 自动载入上一工作日计划，逐项转为完成、推进中、阻塞或取消。
- 允许新增临时任务。
- 每项任务选择项目、分类，可填写多项交付物。
- 至少填写一项实际工作，或选择明确的无工作原因，如请假、培训、会议或无排期。
- 明日计划为空时也必须选择原因。
- 停止输入约 1 秒后自动保存草稿。
- 提交前展示结构化汇总预览。
- 补交后保留“曾逾期”标记。

### 9.2 周报

- 周期为周一至周日，`weekLabel` 取当周周五。
- 截止日为该自然周最后一个工作日，通常为周五；如调休导致周六或周日为工作日，则截止日顺延到该周最后一个工作日。
- 截止日 00:00 从当周已提交日报生成初始草稿。
- 截止日 16:30 预提醒，18:30 标记逾期。
- 员工必须先提交截止日当天的日报，再提交周报；日报提交后刷新周报来源。
- 提交周报时，如当周存在未提交且未免报的日报，系统必须阻止提交并列出缺失日期。
- 自动汇总实际任务、交付物、阻塞和下周计划。
- 员工可合并重复任务、优化表述和补充总结，但不得静默篡改日报事实。
- 周报提交时保存快照。
- 来源日报后续改变时显示“来源已更新”，不自动修改已提交周报。
- 已提交周报只能通过修订产生新版本。

### 9.3 计划滚动

- 未完成计划进入下一周期时，不修改原计划日期。
- 创建带 `sourceTaskId` 的下一周期任务。
- 原任务保留未完成、延期或取消结果。
- 计划兑现率必须基于原计划到期状态计算，不能通过改期抹去延期。

### 9.4 报告修订

- 提交后 7 天内，作者可发起修订并填写原因。
- 超过 7 天需要老板确认后才能生成新版本。
- 修订永不覆盖旧版本。
- 老板修正业务数据必须通过相同的受审计流程。

### 9.5 阻塞处理

- 一般：影响单项任务。
- 重要：影响项目阶段目标；超过 24 小时未处理再次提醒老板。
- 紧急：影响对外承诺或多个项目；立即置顶并要求协调负责人确认接收。
- 老板可以分配协调负责人。
- 提出人可以确认已解决，并填写解决说明。

### 9.6 工作日与免报

- 预置中国大陆年度法定节假日和调休数据。
- 管理员可以覆盖并添加公司特殊日期。
- 管理员可将用户标记为请假或免报；免报不计入逾期率。

## 10. 页面与信息架构

### 10.1 员工端

- 首页 / 今日工作台
- 日报
- 周报
- 项目看板
- 团队动态
- 通知中心
- 个人与安全设置

### 10.2 老板端

- 管理看板
- 成员视角
- 项目视角
- 阻塞中心
- 报告查询
- 通知中心
- 业务变更记录

### 10.3 管理员端

- 用户管理
- 项目与成员管理
- 分类与交付物单位
- 工作日历
- 审计日志
- 系统设置

### 10.4 双视角看板

成员视角展示：

- 提交状态与逾期情况。
- 本周完成、推进中、阻塞和下周计划。
- 个人交付物趋势。
- 个人计划兑现趋势，不用于绩效排名。

项目视角展示：

- 参与成员与项目负责人。
- 本周完成、推进中、阻塞和下周计划。
- 按单位拆分的交付物汇总。
- 阻塞严重级别、协调负责人和处理时长。
- 项目状态和时间范围，不显示无依据的完成百分比。

## 11. 查询、筛选与导出

Release 1 支持按以下条件查询：

- 关键词
- 日期范围
- 成员
- 项目
- 分类
- 任务状态
- 报告状态
- 是否阻塞及阻塞严重级别

筛选结果必须服务端分页并遵循权限边界。

Release 1.1：

- 筛选条件可生成可分享链接。
- 管理员可导入经过模板校验的 Excel。
- 管理员可导出全量 Excel / JSON。
- 老板可导出管理报表。
- 员工只能导出自己的报告。
- 所有导入导出写入审计日志，导出文件标注生成时间与操作者。

## 12. 指标口径

- 按时提交率：截止时间前提交人数 / 应提交人数。
- 计划兑现率：到期且完成的计划任务 / 到期计划任务。
- 阻塞解决时长：从创建到解决的中位时长。
- 交付物数量：按单位分别汇总，禁止跨单位相加。
- 成员负荷：任务数量和阻塞数量，仅用于协调提示。

指标计算必须集中在共享领域服务中，禁止各页面自行实现不同口径。

## 13. 通知规则

Release 1 只使用站内通知：

- 日报：工作日 17:30 预提醒，18:30 逾期。
- 周报：本周最后一个工作日 16:30 预提醒，18:30 逾期；通常为周五。
- 新阻塞：立即进入老板通知中心。
- 重要阻塞：24 小时未处理再次提醒。
- 紧急阻塞：立即置顶并要求确认接收。
- 老板：每天 19:00 生成站内汇总。

后台任务必须具备重复执行保护，避免重复通知。

## 14. 并发、错误与恢复

- 草稿使用版本号进行乐观并发控制。
- 同一账号在多设备编辑且远端已有更新时，停止覆盖并展示双方差异。
- 用户可以选择保留远端、保留本地、合并或另存副本。
- 错误信息必须说明失败动作、原因和可执行的下一步。
- 网络失败时保留未同步草稿，并明确显示状态。
- 禁止静默吞掉数据库、认证、通知或后台任务错误。

## 15. 安全与隐私

- 所有生产流量使用 HTTPS。
- 密钥只通过环境变量或受管密钥注入，不写入仓库和日志。
- 数据库账号使用最小权限。
- 附件下载和对象访问必须鉴权。
- 输入进行服务端校验和输出编码。
- 对导出、账号管理和敏感业务修改实施速率限制与审计。
- 日志不得包含密码、会话令牌或完整敏感报告内容。
- 依赖升级前检查变更、漏洞和兼容性，不使用无范围的自动大版本升级。

## 16. 非功能要求

### 16.1 性能

在不超过 100 名活跃用户、500 个项目的第一版规模下：

- 常用页面在正常网络下 2 秒内可交互。
- 筛选和搜索接口 95% 请求低于 500ms。
- 报告提交低于 1 秒。
- 所有长列表服务端分页。

### 16.2 浏览器

- 支持当前及前两个主要版本的 Chrome、Edge 和 Safari。
- 移动端支持现代 iOS Safari 和 Android Chrome。
- 不支持 Internet Explorer。

### 16.3 数据保留

- 日报、周报、项目和版本历史长期保留。
- 业务审计日志至少保留 3 年。
- 普通登录日志保留 180 天。
- 数据库备份保留 30 天。
- 到期清理由后台任务执行并留下记录。

## 17. 部署与运维

### 17.1 环境

- `development`、`staging`、`production` 使用独立数据库、密钥、附件空间和域名。
- 预发布环境使用生成或脱敏数据。
- 生产环境首次启动只创建初始管理员和基础字典，不写入业务 Mock。

### 17.2 服务器

- 目标为公司 Linux 服务器，使用 Docker Compose。
- 开放 80 / 443，通过公司备案域名访问。
- 优先复用公司现有反向代理；没有现成代理时使用 Caddy 自动管理 HTTPS。
- 数据目录、附件目录和备份目录独立持久化。

### 17.3 发布与回滚

- 应用使用带版本号的不可变容器镜像。
- 发布前完成数据库备份。
- 数据库迁移必须向前兼容。
- 破坏性 schema 变更拆成多次发布。
- 应用失败时回滚镜像，不自动执行破坏性数据库降级。
- 每次生产发布对应明确 Git 提交和版本标签。

### 17.4 备份与监控

- 数据库每日自动备份，并复制到服务器之外。
- 备份保留 30 天，定期进行恢复演练。
- 目标恢复点不超过 24 小时，目标恢复时间不超过 4 小时。
- 监控登录异常、服务错误率、数据库连接、磁盘容量、后台任务、备份结果和证书到期。
- 连续错误、备份失败、磁盘不足和证书临近到期必须主动告警运维。
- 第一版不承诺高可用集群。

## 18. 测试与质量门禁

任何代码进入生产前必须通过：

- TypeScript 严格类型检查。
- Lint 和格式检查。
- 领域规则单元测试。
- PostgreSQL 数据访问与迁移集成测试。
- 登录、改密、管理员重置密码和会话撤销测试。
- 角色权限和资源归属越权测试。
- 日报、周报、计划滚动、修订和阻塞流程测试。
- Playwright 关键路径端到端测试。
- 生产构建。

关键端到端场景至少包括：

1. 管理员使用正式密码创建员工，员工直接登录并提交日报。
2. 昨日计划进入今日，状态和来源链路正确。
3. 周报从日报生成，提交后保持快照。
4. 员工无法读取他人草稿，管理员也不能绕过业务权限。
5. 老板处理阻塞并产生正确通知和审计。
6. 报告修订保留旧版本和原因。
7. 并发编辑不会静默覆盖远端数据。
8. 生产环境不会自动写入 Mock 数据。
9. 备份能够在隔离环境恢复。

## 19. Release 1 验收标准

Release 1 只有同时满足以下条件才能视为完成：

- 三种角色可以按权限完成各自核心工作流。
- 所有业务读取与写入均有服务端授权。
- 日报、周报、计划、交付物和阻塞关系可追溯。
- 周报快照、报告版本和审计记录不可被静默覆盖。
- 工作日、请假、免报和逾期计算符合规则。
- 双视角看板口径一致并可下钻。
- 站内通知不会重复发送。
- 开发、预发布、生产配置隔离。
- 自动化质量门禁全部通过。
- 生产构建成功。
- 备份与恢复演练通过。
- 无明文凭证、默认密码、测试后门或生产 Mock 数据。
- 操作手册包含部署、回滚、备份恢复和初始管理员创建步骤。

## 20. 实施顺序

1. 初始化 Git，建立现有文件基线。
2. 创建 Next.js / TypeScript 工程与质量门禁。
3. 接入 BEUI Pro 私有 Registry，建立语义主题、共享组件和页面壳层。
4. 建立 PostgreSQL schema、迁移和种子策略。
5. 实现账号、密码、会话和角色权限。
6. 实现项目、分类、单位和工作日历后台。
7. 按测试优先方式实现日报、任务和计划滚动。
8. 实现周报生成、快照和修订版本。
9. 实现阻塞中心、通知和审计。
10. 实现成员视角、项目视角、搜索和指标。
11. 完成安全、性能、无障碍和端到端验证。
12. 在预发布环境完成迁移、备份和恢复演练后发布 Release 1。
13. Release 1 稳定后再实现 Release 1.1。

## 21. 上线前必须核实的外部条件

这些不是产品设计缺口，但实施和部署前必须取得真实证据：

- 公司服务器的操作系统、CPU、内存、磁盘和 Docker 可用性。
- 域名、DNS 管理权限和 HTTPS 证书策略。
- 80 / 443 端口和防火墙策略。
- PostgreSQL 与备份目标的存储容量。
- 服务器外备份位置和恢复负责人。
- 监控告警的实际接收渠道。
- Release 1.1 对象存储地址、凭证与容量限制。

未经验证不得声称系统已经部署、可公网访问、完成备份或通过恢复演练。
