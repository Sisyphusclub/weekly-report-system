# 开发与验收进度

验收依据：`CODEX_SPEC.md`。更新日期：2026-09-18。

## 本轮完成

- 老板业务变更入口 `/activity`：组织隔离、资源可见性、草稿事件隔离，与管理员审计复用列表。
- 修复 GET 读取及导出被 Origin 校验误拦截；写入仍要求同源 Origin，并保留身份、安全设置与限流检查。
- 报告查询支持成员、类型、状态和日期组合；分页、分享及导出复用条件；成员看板按 ID 下钻。
- 老板今日提交清单：待交、逾期、已交、补交、免报、休息日与分页，不展示他人草稿正文。
- 成员和团队提交指标共用工作日、免报、入职日期及截止时间口径；复用提交数据读取。
- 管理员系统设置：组织名称、并发版本保护、修改原因与事务审计；运行配置仅显示配置状态。
- 新增迁移 `0014_organization_settings.sql`，部署前必须执行迁移。
- 项目视角补充负责人、参与成员和本周计划任务，均从任务与项目成员关系读取并保留任务下钻入口。
- 任务状态变更新增不可变历史表，为计划到期兑现率提供状态时间证据；新增迁移 `0015_task_status_history.sql`。
- 任务和阻塞评论支持回复关系，回复目标限定在同一任务/阻塞及同一组织；新增迁移 `0016_comment_replies.sql`。
- 看板新增本周阻塞每日新增/解决趋势，按当前角色可见的阻塞数据计算。
- 计划兑现率已按计划到期日读取状态历史，在到期时点判断是否完成，避免使用当前状态倒推。
- 看板新增成员交付物聚合，按单位分组并提供任务下钻，不跨单位相加。

## 已确认仍需开发或补齐

1. 管理看板基础交付物聚合、阻塞趋势、中位时长和计划兑现率已完成；项目视角与阻塞趋势已支持日期范围，真实认证后的历史数据仍需验收。
2. 项目视角已支持日期范围筛选；负责人、参与成员和计划任务展示已完成，所有新增管理指标须能下钻原文。
3. 评论回复的端到端真实认证流程仍需验收；接口、数据模型与页面入口已完成。
4. 报告列表支持按成员、类型、状态、项目、分类、任务状态及是否阻塞筛选；真实认证后的跨角色流程仍需验收。
5. 普通登录日志已增加可重复执行的 180 天清理任务（`npm run login-audit:cleanup`，可用环境变量调整）；备份创建、恢复和最近备份校验脚本已具备（`npm run backup:check -- -BackupDirectory <目录>`）；`scripts/run-scheduled-jobs.ps1` 提供提醒、周报草稿、登录审计和附件清理的统一调度入口，仍需真实计划任务执行证据与运维告警接入。
6. 多设备草稿冲突处理、周报来源变更和跨角色关键流程仍需完整验收；现有代码与单元测试不等于这些场景全部通过。

## 验证边界

- 本地单元及接口模拟测试、类型检查、Lint、格式检查和生产构建已通过；最新具体数量以本轮运行输出为准。
- Playwright 当前覆盖未配置环境的登录页、移动布局、保护路由重定向和健康检查；未覆盖真实认证后的业务流程。
- PostgreSQL 集成门禁已经包含迁移重放、组织约束、并发更新、不可变历史和业务审计可见性，但本地尚无数据库，未宣称实测通过。
- S3、恶意文件扫描、Docker Compose、真实备份恢复、性能与完整无障碍验收仍需真实环境证据。
- 用户已确认先继续开发，不等待 PostgreSQL 准备；不得用 Mock 数据冒充持久化业务结果。
- 新增 `OPERATIONS.md`，覆盖部署、回滚、备份恢复和初始管理员创建；真实环境演练仍需执行记录。
- 健康检查返回数据库与对象存储分项状态，便于监控定位故障且不暴露敏感配置。
- 新增 `npm run verify` 统一执行类型、Lint、格式、单元测试、迁移检查和生产构建门禁。
- 新增 `npm run verify:e2e`，在核心门禁通过后追加 Playwright 验收。
- 最近一次 `npm run verify` 已通过核心门禁、286 个单元测试和生产构建；Docker 生产形态已完成 `app + db` 启动与迁移验证。使用空闲端口运行时，数据库状态为 `ready`；对象存储和扫描服务因生产 Compose 依赖外部注入而显示 `unavailable`，不能据此宣称生产外部服务已验收。
- 开发基础设施真实集成验收已通过：PostgreSQL、MinIO、ClamAV 启动健康，`GET /api/health` 返回四项 `ready`；存储扫描测试通过鉴权、干净文件、EICAR 拦截、大小不一致拒绝和对象清理。
- 新增 `npm run config:check`，用于部署前检查配置完整性且不输出凭据。

只有 `CODEX_SPEC.md` 第 19 节全部验收条件都有证据时，才能将 Release 1 标记为完成。
