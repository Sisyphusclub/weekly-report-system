# 市场部工作看板系统：前端完整实施方案 (Release 1)

> **文档定位**：本规范作为前端完整重构与 Codex 交付的唯一依据。全面抛弃原原型中无主次的平铺样式，基于生产级规范（`CODEX_SPEC.md`）与业务数据闭环重新推导排版[cite: 2]。
> **业务流转更新**：去除跨日“明日计划”，工作计划仅保留**“今日工作计划”**，实现同日内“早晨定计划 ➔ 下班填实际并核销 ➔ 暴露卡点”的高效闭环。
> **技术依赖约束**：基于 Next.js + TypeScript 开发，严禁从外部虚拟 npm 包引用，所有 UI 控件必须复用本地 `@/components/...`（beUI Pro 源码）[cite: 2]。

---

## 一、 全局架构与布局框架 (App Shell)

系统统一采用 **“紧凑左侧导航栏 + 顶部情境控制栏 + 弹性主工作区”** 的三段式布局[cite: 2]。

### 全局页面结构布局线框图

```text
+---------------------+----------------------------------------------------------+
| 侧边导航 (Sidebar)   | 顶部控制栏 (Top Navigation Bar)                          |
+---------------------+----------------------------------------------------------+
| [Logo 产业平台部]    | 面包屑 / 当前工作日状态 / 倒计时药丸 / 阻碍告警铃铛 / 用户与角色头像 |
+---------------------+----------------------------------------------------------+
| - 今日工作台/日报   |                                                          |
| - 本周周报与滚动     |                                                          |
| - 项目协同看板       |                       主工作区 (Main View)               |
| - 成员双栏看板       |                     (支持自适应与响应式断点)             |
| - 阻塞作战中心       |                                                          |
| - 后台配置 (Admin)  |                                                          |
+---------------------+----------------------------------------------------------+
```

### 1. 颜色语义与视觉设计系统
* **完成态 (`DONE`)**：文本/图标 `#26c281`，背景浅底 `rgba(38,194,129,0.12)`[cite: 2, 3]。
* **推进中 / 计划态 (`IN_PROGRESS` / `PLAN`)**：主题蓝 `#4f8cff`，计划黄褐 `#e5a13a`（浅橙底 `rgba(229,161,58,0.14)`）[cite: 2, 3]。
* **待跟进 / 延期**：状态橙 `#ffb020`，背景 `rgba(255,176,32,0.14)`[cite: 2, 3]。
* **风险 / 阻塞 (`BLOCKED`)**：警示红 `#ff4d4f`，浅红背景 `rgba(255,77,79,0.12)`，配合红色描边与感叹号图标强提示[cite: 2]。
* **排版原则**：信息层级以“固定两行式列表”、“左右对比分区”以及“侧滑抽屉（Drawer）”展开为主，严禁仅依靠色彩传递状态，必须保持“图标 + 颜色 + 状态文字”三要素[cite: 2, 3]。

---

## 二、 页面详细排版与交互规格

### 页面 1：今日工作台 / 日报填报页 (`/daily`)

* **页面排版形态**：非对称左右分栏（左 65% 主输入与核销流程，右 35% 阻碍申报与当前进度）。
* **布局与元素拆解**：
  * **顶部情境栏 (Context Header)**：
    * 左侧：显示大号当前日期（如 `2026.09.18 星期五`）及绿色“工作日”徽标[cite: 2, 3]。
    * 右侧：距离 18:30 提交截止倒计时药丸（超时显示“曾逾期”标签）与防抖自动保存状态点（“草稿已同步”）[cite: 2]。
  * **左主栏：核心填报区 (Canvas)**：
    * **模块 A：【今日工作计划】(Today's Plan)**：
      * 适用场景：早晨上班或工作开始时快速拟定今日待办。
      * 条目卡片字段：计划描述、所属项目 Select、预计产出物说明（如 `视频1条`、`用例20条`）[cite: 2, 3]。
      * 快捷操作：支持回车快速新增下一条。
      * 底部按钮：`一键将计划转入今日实际核销`。
    * **模块 B：【今日实际完成】(Today's Accomplishment)**[cite: 2]：
      * 适用场景：下午/下班前对今日计划进行状态勾选与交付物补充[cite: 2]。
      * 条目设计（若由今日计划转入，自动带出项目与描述）：
        * 第一行：所属项目 Select（带搜索）、分类 Select（支持自动推断归类标签）、删除图标[cite: 2]。
        * 第二行：自适应高度工作内容 Textarea[cite: 2]。
        * 第三行：交付物量化输入（数值 Input + 单位 Select，如 `2` + `个接口`）及状态切换单选按钮组（`已完成` / `推进中` / `已取消`）[cite: 2, 3]。
      * 底部按钮：`+ 添加今日临时插入任务`（处理非计划内的临时插队事务）[cite: 2]。
  * **右侧栏：阻碍暴露与计划核销指示 (Sidebar Box)**：
    * **阻塞协调常驻卡 (Blocker Box)**[cite: 2]：
      * 开关：`[ 进展顺利 / 无卡点 ]` vs `[ 存在阻塞 / 需协调 ]`[cite: 2]。
      * 勾选“存在阻塞”时展开必填项：所属项目 Select、严重度单选（`一般` / `重要` / `紧急`）、阻碍具体原因 Textarea[cite: 2]。提交后全局广播该项目受阻[cite: 2]。
    * **今日计划履约率实时指示器**：
      * 动态环形进度图：实时显示今日拟定的计划中，当前已核销完成的比例（如 `3/4 项已完成`）。
* **底部固定操作条**：
  * 统计指示器（今日计划 X 项，已完成 Y 项，阻塞 Z 项）。
  * 主操作按钮：`[ 预览并提交日报 ]`（触发 Modal 结构化预览，确认后更新为 `SUBMITTED` 写入后端）[cite: 2]。

---

### 页面 2：本周周报与计划滚动页 (`/weekly`)

* **页面排版形态**：三段式渐进卡片流（日报聚合 ➔ 提炼总结 ➔ 下周规划）[cite: 2]。
* **前置拦截校验**：
  * 若本周内（周一至周五）存在未提交且未免报的日报，顶部出现全宽黄色警示横幅，并禁用周报提交按钮[cite: 2]。
* **布局与元素拆解**：
  * **第 1 阶段：本周实际工作聚合 (From Daily Reports)**[cite: 2]：
    * 提供 **「从本周日报一键汇总」** 按钮[cite: 2]。
    * 聚合后以 **“项目”** 为维度折叠展示，系统自动合并同类条目并统计各单位交付物（如 `用例 30条 · 接口 4个`）[cite: 2, 3]。
    * 员工可在条目中直接润色文案、合并条目，保留快照[cite: 2]。
  * **第 2 阶段：未闭环阻塞复盘**[cite: 2]：
    * 汇总本周日报所有 `BLOCKED` 项，逐一确认状态（标记为“本周已解除”或“带入下周继续跟进”）[cite: 2]。
  * **第 3 阶段：下周重点工作规划 (Next Week Milestones)**[cite: 2]：
    * 结构化表格形式录入周度里程碑：`归属项目` | `里程碑任务` | `预期产出物` | `预计截止日`[cite: 2]。
* **版本快照控制**：
  * 提交后锁定为不可修改版本快照 `ReportRevision`；若后续底层日报有改动，周报顶部提示“来源日报已更新，可申请修订”[cite: 2]。

---

### 页面 3：老板端 · 管理总览驾驶舱 (`/boss/dashboard`)

* **页面排版形态**：核心指标置顶 ➔ 异常卡点作战 ➔ 每日全景矩阵[cite: 2]。
* **布局与元素拆解**：
  * **第 1 行：核心健康度度量卡片 (Top Metric Cards)**[cite: 2]：
    * **提交进度卡**：如 `13/14 已提交 (92%)`，点击右侧滑出抽屉展示未交名单与催办操作[cite: 2, 3]。
    * **阻塞警报卡**：如 `3 项待协调`（鲜红高亮，包含紧急项角标）[cite: 2]。
    * **今日计划兑现率卡**：展示当天各成员“今日计划 ➔ 今日完成”的即时达成率。
    * **累计交付物卡**：按单位分类统计的标签气泡群[cite: 2]。
  * **第 2 行：老板专属 · 阻塞作战室 (Blocker Stream)**[cite: 2]：
    * 横向警示列表，展示处于 `OPEN` 与 `ACKNOWLEDGED` 的项目阻碍[cite: 2]。
    * 卡片显示：紧急度 Tag、所属项目、提出人姓名、停滞时长、卡点描述[cite: 2]。
    * 核心操作：点击 `[ 指派协调人 ]` 弹出人员选择框，指派后实时发送站内通知并将状态流转至“协调中”[cite: 2]。
  * **第 3 行：今日全景分析 (双列排布)**[cite: 3]：
    * **左列 (60%) 本日工作计划横向矩阵**：按人员分列展示每人当天的待办计划，支持横向鼠标滚轮滑动与拖拽[cite: 3]。
    * **右列 (40%) 工作分类与产出物下钻**：展示分类精力分布条形图与带单位的交付物汇总，点击任意分类可展开溯源条目[cite: 1, 3]。

---

### 页面 4：老板端 · 成员视角看板 (`/boss/members`)

* **页面排版形态**：双栏卡片网格系统（Card Grid），每位员工独立一张大卡片。
* **网格配置**：`grid-template-columns: repeat(auto-fill, minmax(360px, 1fr))`。
* **卡片结构拆解 (`MemberCompareCard.tsx`)**：
  * **卡片头部 (Header)**：
    * 员工彩色头像、姓名、岗位职称[cite: 1, 2, 3]。
    * 状态胶囊徽标：按时提交（绿）/ 缺报（灰）/ 曾逾期（橙）[cite: 2, 3]。
    * 风险警示条：若该员工日报存在卡点，卡片外边框亮红框并在头部高亮卡点描述[cite: 2]。
  * **卡片主体：双栏对照区 (Body)**：
    * **左半区【今日实际交付】**[cite: 2, 3]：
      * 单条格式（固定两行）：第一行显示序号 + 内容描述 + 分类徽标；第二行换行显示量化产出 Badge + 状态标签（完成/进行中）[cite: 3]。
    * **右半区【今日工作计划】**[cite: 2, 3]：
      * 列出该员工早晨填写的今日工作计划，已在左侧完成核销的标记绿勾，未完成的标记橙色待跟进[cite: 2, 3]。
  * **卡片底部 (Footer)**：
    * 展示该成员今日计划的达成率进度条，以及个人分类精力占比小进度条[cite: 1, 2, 3]。
* **详情滑动抽屉 (Drawer)**：
  * 点击卡片任意区域，从右侧滑出抽屉，按自然日时间轴详细回溯该员工历史填报[cite: 2]。

---

### 页面 5：老板端 · 项目视角看板 (`/boss/projects`)

* **页面排版形态**：项目维度折叠卡片列表（以项目为主体，聚合跨成员贡献）[cite: 2]。
* **卡片结构拆解 (`ProjectCollabCard.tsx`)**：
  * **项目头部 (Header)**：
    * 项目名称（加粗大号字体）+ 状态徽标（`进行中` / `筹备中` / `已归档`）[cite: 2]。
    * 协同成员头像群（AvatarGroup 重叠展示所有参与人）[cite: 2]。
    * 累计交付物总量汇总（严格按单位独立统计，如 `原型 1套 · 接口 4个 · 文件 5份`）[cite: 2, 3]。
    * 风险指示：若项目内含阻塞，标题右侧出现醒目的红色感叹号和卡点人摘要[cite: 2]。
  * **折叠展开区 (Accordion Details)**：
    * **成员协同分工列表**：按“人员”分组展示其负责事项（例如：于继鹏负责接口联调、李雪负责功能测试）[cite: 2, 3]。
    * **今日项目计划池**：聚合各成员今天设立的与该项目相关的计划待办项[cite: 2]。

---

### 页面 6：管理员后台维护页面 (`/admin/*`)

采用紧凑高密度的 beUI Pro Table 构建[cite: 2]：
1. **用户与权限 (`/admin/users`)**：维护员工账号生命周期，生成一次性初始密码，停用离职人员（软删除，永久保留报告署名与历史数据）[cite: 2]。
2. **项目管理 (`/admin/projects`)**：维护项目立项与归档（归档项目禁止新任务关联）[cite: 2]。
3. **字典与日历 (`/admin/dictionaries` & `/admin/calendar`)**：配置分类字典与交付物单位；维护国家法定节假日、公司调休及员工免报/请假记录（免报日期自动从逾期统计中核减）[cite: 2]。
4. **系统审计日志 (`/admin/audit`)**：只读分页表格，记录关键业务变更与权限操作上下文[cite: 2]。

---

## 三、 核心组件设计与代码契约

### 1. 工作项强制两行式行组件 (`TaskItemRow.tsx`)
```tsx
import React from 'react';
import { Badge } from '@/components/ui/badge';
import { Pill } from '@/components/ui/pill';

export interface TaskItemProps {
  index: number;
  content: string;
  projectName?: string;
  category: string;
  categoryColor: string;
  deliverableText?: string;
  status: 'DONE' | 'IN_PROGRESS' | 'BLOCKED';
  isPlan?: boolean;
}

export const TaskItemRow: React.FC<TaskItemProps> = ({
  index,
  content,
  projectName,
  category,
  categoryColor,
  deliverableText,
  status,
  isPlan = false,
}) => {
  return (
    <li className={`flex flex-wrap items-start gap-x-2 gap-y-1 py-1.5 min-w-0 text-sm ${isPlan ? 'bg-amber-50/60 dark:bg-amber-950/20 px-2 rounded' : ''}`}>
      {/* 第一行：序号 + 内容 + 钉在行尾右上角的分类徽标 */}
      <span className="font-mono text-blue-500 font-semibold flex-shrink-0 w-4">{index + 1}</span>
      <div className="flex-1 min-w-0 break-words leading-relaxed text-slate-800 dark:text-slate-200">
        {projectName && (
          <span className="inline-block px-1.5 py-0.5 mr-1.5 text-xs rounded bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-400 font-medium">
            {projectName}
          </span>
        )}
        {content}
      </div>
      <Badge borderColor: categoryColor categoryColor, className="flex-shrink-0 self-start text-xs border" color: style="{{" variant="outline" }}>
        {category}
      </Badge>

      {/* 第二行：强制换行元数据块（产出物 + 状态胶囊） */}
      {(deliverableText || status) && (
        <div className="flex-[0_0_100%] flex items-center gap-2 pl-6 mt-0.5 min-w-0">
          {deliverableText && (
            <span
              className="flex-1 min-w-0 truncate text-xs text-blue-600 dark:text-blue-400 bg-blue-50 dark:bg-blue-950/40 px-2 py-0.5 rounded"
              title={deliverableText}
            >
              产出：{deliverableText}
            </span>
          )}
          <Pill className="flex-shrink-0 ml-auto" status="{status}"/>
        </div>
      )}
    </li>
  );
};
```

### 2. 本日计划横向滑动组件 (`PlanStrip.tsx`)
```tsx
import React, { useRef, useState, useEffect } from 'react';

export const PlanStrip: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const stripRef = useRef<HTMLDivElement>(null);
  const [canLeft, setCanLeft] = useState(false);
  const [canRight, setCanRight] = useState(false);

  const checkScroll = () => {
    if (!stripRef.current) return;
    const { scrollLeft, scrollWidth, clientWidth } = stripRef.current;
    setCanLeft(scrollLeft > 2);
    setCanRight(scrollLeft < scrollWidth - clientWidth - 2);
  };

  useEffect(() => {
    checkScroll();
    window.addEventListener('resize', checkScroll);
    return () => window.removeEventListener('resize', checkScroll);
  }, []);

  const handleWheel = (e: React.WheelEvent) => {
    if (!stripRef.current) return;
    const { deltaX, deltaY } = e;
    const delta = Math.abs(deltaX) > Math.abs(deltaY) ? deltaX : deltaY;
    stripRef.current.scrollLeft += delta;
  };

  return (
    <div className="relative w-full overflow-hidden">
      {/* 边缘渐变提示遮罩 */}
      <div
        className={`pointer-events-none absolute left-0 top-0 bottom-0 w-8 bg-gradient-to-r from-amber-50 dark:from-slate-900 to-transparent transition-opacity z-10 ${
          canLeft ? 'opacity-100' : 'opacity-0'
        }`}
      />
      <div
        className={`pointer-events-none absolute right-0 top-0 bottom-0 w-8 bg-gradient-to-l from-amber-50 dark:from-slate-900 to-transparent transition-opacity z-10 ${
          canRight ? 'opacity-100' : 'opacity-0'
        }`}
      />

      {/* 滚动容器 */}
      <div
        ref={stripRef}
        onScroll={checkScroll}
        onWheel={handleWheel}
        className="flex gap-3 overflow-x-auto pb-2 pt-1 scrollbar-thin scrollbar-thumb-amber-200 cursor-grab active:cursor-grabbing"
      >
        {children}
      </div>
    </div>
  );
};
```

---

## 四、 前端路由与目录结构清单

```text
src/
├── app/
│   ├── layout.tsx                     # 根布局与 Provider 注入
│   ├── (auth)/
│   │   └── login/page.tsx             # 登录与 TOTP 绑定
│   ├── (workspace)/
│   │   ├── daily/page.tsx             # 日报工作台（今日计划 + 今日实际核销）
│   │   └── weekly/page.tsx            # 周报汇总与规划
│   ├── (dashboard)/boss/
│   │   ├── dashboard/page.tsx         # 老板管理总览驾驶舱
│   │   ├── members/page.tsx           # 成员双栏对比看板
│   │   ├── projects/page.tsx          # 项目协同看板
│   │   └── blockers/page.tsx          # 阻塞协调作战中心
│   └── admin/
│       ├── users/page.tsx             # 用户与账号管理
│       ├── projects/page.tsx          # 项目生命周期管理
│       ├── dictionaries/page.tsx      # 分类与单位字典
│       ├── calendar/page.tsx          # 工作日历与免报审批
│       └── audit/page.tsx             # 审计日志查询
├── components/
│   ├── ui/                            # 复用本地 beUI Pro 组件
│   ├── layout/
│   │   ├── AppShell.tsx               # 侧边栏 + 顶栏壳层
│   │   ├── Sidebar.tsx                # 权限隔离动态菜单
│   │   └── TopNav.tsx                 # 倒计时、状态点与告警中心
│   ├── daily/
│   │   ├── TodayPlanList.tsx          # 今日工作计划拟定列表
│   │   ├── TodayWorkForm.tsx          # 今日实际核销与录入卡片列表
│   │   └── BlockerBox.tsx             # 阻塞申报高亮卡片
│   ├── weekly/
│   │   ├── DailyAggregator.tsx        # 一键汇总与去重合并器
│   │   └── WeeklyPlanTable.tsx        # 下周周度规划表格
│   └── dashboard/
│       ├── BlockerStream.tsx          # 阻塞作战室横向卡片列表
│       ├── MemberCompareCard.tsx      # 成员双栏对照卡片（今日计划 vs 今日完成）
│       ├── ProjectCollabCard.tsx      # 项目协同折叠面板卡片
│       ├── PlanStrip.tsx              # 今日计划横向滑动容器
│       └── TaskItemRow.tsx            # 固定两行式规范工作项
└── types/
    └── index.ts                       # 与后端严格对齐的 TypeScript 实体
```

---

## 五、 Codex 实施验收标准

1. **结构严谨**：所有任务条目必须严格满足“固定两行”排布规则，严禁长文本换行导致卡片错乱[cite: 3]。
2. **同日闭环**：日报填报支持早晨拟定今日计划，下班前一键划转并核销为今日实际完成[cite: 2]。
3. **状态联动真实**：日报申报 `BLOCKED` 时，老板端管理驾驶舱与项目看板必须实时亮起红框并能直接进行指派操作[cite: 2]。
4. **周报聚合顺畅**：周五周报一键从周一至周五日报拉取聚合、核算产出物总量[cite: 2, 3]。
5. **组件合规**：所有表单控件均通过本地 `@/components/...` 引入，无任何孤立的原生 HTML 标签及无范围样式[cite: 2]。
