import { and, asc, desc, eq, ne } from "drizzle-orm";
import { notFound } from "next/navigation";
import { requireUser } from "@/lib/access"; import { getDb } from "@/lib/db"; import { category, project, user, workTask } from "@/lib/db/schema"; import { WorkspaceShell } from "@/components/workspace/shell"; import { TaskForm } from "@/components/workspace/task-form";
export const metadata = { title: "任务管理" };
export default async function TasksPage() { const actor = await requireUser(); if (actor.role === "ADMIN") notFound(); const db = getDb(); const [projects, categories, people, tasks] = await Promise.all([db.select({ id: project.id, name: project.name }).from(project).where(and(eq(project.organizationId, actor.organizationId), ne(project.status, "ARCHIVED"))).orderBy(asc(project.name)), db.select({ id: category.id, name: category.name }).from(category).where(and(eq(category.organizationId, actor.organizationId), eq(category.enabled, true))).orderBy(asc(category.sortOrder), asc(category.name)), db.select({ id: user.id, name: user.name }).from(user).where(and(eq(user.organizationId, actor.organizationId), eq(user.status, "ACTIVE"), ne(user.role, "ADMIN"))).orderBy(asc(user.name)), db.select({ id: workTask.id, content: workTask.content, kind: workTask.kind, status: workTask.status, dueDate: workTask.dueDate, categoryName: workTask.categoryName }).from(workTask).where(and(eq(workTask.organizationId, actor.organizationId), actor.role === "EMPLOYEE" ? eq(workTask.primaryAssigneeId, actor.id) : undefined)).orderBy(desc(workTask.updatedAt), desc(workTask.id)).limit(50)]); return <WorkspaceShell actor={actor} selected="tasks"><h1 className="text-title-1-medium">任务管理</h1><TaskForm projects={projects} categories={categories} people={people} selfId={actor.id} /><section className="flex flex-col gap-3" aria-label="任务列表"><h2 className="text-title-2-medium">最近任务</h2>{tasks.length === 0 ? <p>暂无任务</p> : tasks.map((task) => <div key={task.id} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-border-button-default p-4"><span className="min-w-0 break-words">{task.content}</span><span className="text-body-regular text-text-secondary">{task.kind === "PLAN" ? "计划" : "实际"} · {task.categoryName} · {{ TODO: "待开始", IN_PROGRESS: "进行中", BLOCKED: "阻塞", DONE: "完成", CANCELED: "已取消" }[task.status]}{task.dueDate ? ` · ${task.dueDate}` : ""}</span></div>)}</section></WorkspaceShell>; }
